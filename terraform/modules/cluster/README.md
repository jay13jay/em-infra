# terraform/modules/cluster

Scaffold for reusable Terraform cluster module (cloud-agnostic). Include inputs/outputs, provider configuration, and remote state guidance here.

Starter tasks:
- add `main.tf`, `variables.tf`, `outputs.tf` when ready
- document backend (S3/GCS/remote state) and required provider versions
