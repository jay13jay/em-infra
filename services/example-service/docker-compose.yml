version: '3.8'
services:
  example-service:
    build: .
    image: ghcr.io/your-org/example-service:dev
    volumes:
      - .:/usr/src/app:delegated
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    command: npm run start:dev
