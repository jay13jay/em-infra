[templates]
# Replace with the temporary VM IP (or hostname) you create from the template
# Example:
# 192.0.2.10 ansible_user=ubuntu ansible_ssh_private_key_file=/path/to/key

# Example placeholder (edit before running):
# 10.0.0.69 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa
